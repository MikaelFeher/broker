.jumbotron#index
  %h1 Micke Mäklare
  %h2 Välkommen in!
  %h3 Ni kanske hittar ert nästa hus i Skurup eller Beddingestrand.
  %br
  %br
  %br
  %br
  %div
    - Category.all.each do |category|
      .btn.btn-primary{style:'text-shadow: none; background-color: white; width: 130px;'}
        = link_to category.name, products_path(category: category.name)
  %br
  %br

.container{style:'padding: 0px; margin: 0px; text-align:center'}
  %h1
    = params[:category]

  #bs-example-navbar-collapse-1.collapse.navbar-collapse
    %ul.nav.navbar-nav
      %li.dropdown
        %a.dropdown-toggle{"aria-expanded" => "false", "aria-haspopup" => "true", "data-toggle" => "dropdown", :href => "#", :role => "button"}
          Välj kommun
          %span.caret
            %ul.dropdown-menu
              - Category.all.each do |category|
                %li
                  = link_to category.name, products_path(category: category.name)

  = render 'houses'    